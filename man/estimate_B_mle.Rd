% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hiddenCOMET_functions.R
\name{estimate_B_mle}
\alias{estimate_B_mle}
\title{Estimate emission matrix using maximum likelihood}
\usage{
estimate_B_mle(
  Theta,
  P,
  observed_states,
  hidden_states,
  N = 2000L,
  starts = 20L,
  method = "BFGS"
)
}
\arguments{
\item{Theta}{Observed state proportions matrix (n_obs × n_timepoints)}

\item{P}{Hidden state proportions matrix (n_hidden × n_timepoints)}

\item{observed_states}{Character vector of observed state names}

\item{hidden_states}{Character vector of hidden state names}

\item{N}{Pseudo-count for multinomial likelihood (default: 2000)}

\item{starts}{Number of random restarts (default: 20)}

\item{method}{Optimization method: "BFGS" (default: "BFGS")}
}
\value{
Estimated emission matrix B (n_obs × n_hidden)
}
\description{
Estimates the emission matrix B using maximum likelihood estimation
with multinomial likelihood.
}
\examples{
Theta <- matrix(runif(12), 3, 4)
P <- matrix(runif(12), 3, 4)
observed_states <- c("G1", "S", "G2M")
hidden_states <- c("E", "H", "M")
B_est <- estimate_B_mle(Theta, P, observed_states, hidden_states)
}
